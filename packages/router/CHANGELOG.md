# @tuttiai/router

## 0.1.0

### Minor Changes

- Initial scaffold — types, `HeuristicClassifier` with destructive-tool awareness, and the `Classifier` / `SmartProviderConfig` / `RoutingDecision` public surface. The `SmartProvider` `LLMProvider` implementation lands in a later release.
